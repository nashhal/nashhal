name: Refresh NOVEN knowledge feed

on:
  workflow_dispatch:
  schedule:
    - cron: "17 * * * *"

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build feed
        run: python3 scripts/build_ai_feed.py

      - name: Commit feed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ai/data/live-news.json
          git diff --cached --quiet && exit 0
          git commit -m "chore(ai): refresh NOVEN knowledge feed"
          git push
