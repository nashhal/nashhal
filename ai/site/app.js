(() => {
  const config = window.NASHHAL_AI_CONFIG || {};
  const API_BASE = typeof config.apiBase === 'string' ? config.apiBase.trim() : '';
  const form = document.getElementById('chatForm'); const question = document.getElementById('question'); const send = document.getElementById('send'); const result = document.getElementById('result'); const answer = document.getElementById('answer'); const sources = document.getElementById('sources'); const count = document.getElementById('charCount'); const historyList = document.querySelector('.history-list');
  if (!form || !question || !send || !result || !answer || !sources) { console.error('NOVEN: required UI elements are missing.'); return; }
  const history=[]; const sleep=(ms)=>new Promise((resolve)=>setTimeout(resolve,ms)); const lang=()=>window.NOVEN_I18N?.language || (navigator.language||'en').split('-')[0]; const isRtl=()=>['ar','fa','ur','he'].includes(lang());
  const builtIn=[
    {key:'noven',keys:['what is noven','what does noven','nov en'],title:'NOVEN',source:'NOVEN',url:'./',text:'NOVEN is a global intelligence workspace for asking questions, retrieving evidence, comparing sources, and creating a verifiable record of the result.'},
    {key:'ai',keys:['what is ai','what is artificial intelligence','artificial intelligence'],title:'Artificial intelligence',source:'Reference',url:'https://en.wikipedia.org/wiki/Artificial_intelligence',text:'Artificial intelligence is the field of building computer systems that perform tasks commonly associated with human intelligence, including learning, reasoning, perception, language understanding, and decision-making.'},
    {key:'web3',keys:['web3','blockchain','on chain','on-chain'],title:'Web3',source:'NOVEN',url:'./#web3',text:'NOVEN uses Web3 as a trust layer for identity, provenance, signatures, attestations, and future agent-to-agent services.'},
    {key:'arch',keys:['github pages','gpu','local model','qwen'],title:'NOVEN architecture',source:'NOVEN',url:'./#system',text:'GitHub Pages serves the interface. A full language model needs compute outside static hosting, so NOVEN combines browser retrieval with public references and keeps a path open for a self-hosted model.'},
    {key:'evidence',keys:['retrieval','search','source','sources','evidence'],title:'Retrieval and evidence',source:'NOVEN',url:'./#research',text:'NOVEN ranks available knowledge records against the question, selects the strongest matches, and keeps supporting evidence attached to the response.'},
    {key:'hello',keys:['hello','hi','مرحبا','السلام','hola','bonjour'],title:'NOVEN',source:'NOVEN',url:'./',text:'Hello. I’m NOVEN. Ask a question and I’ll search the available knowledge and public reference sources.'}
  ];
  function normalize(v){return String(v||'').toLowerCase().replace(/[\u064B-\u065F\u0670]/g,'').replace(/[إأآا]/g,'ا').replace(/[ىي]/g,'ي').replace(/ة/g,'ه').replace(/[^a-z0-9\u0600-\u06ff]+/gi,' ').trim();}
  function escapeHtml(v){return String(v??'').replace(/[&<>\"]/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));}
  function addHistory(q){if(!historyList||history.includes(q))return;history.unshift(q);history.splice(12);historyList.innerHTML='';history.forEach((item)=>{const b=document.createElement('button');b.type='button';b.className='history-item';b.textContent=item;b.addEventListener('click',()=>{question.value=item;updateCounter();question.focus();});historyList.appendChild(b);});}
  function updateCounter(){if(count)count.textContent=String(question.value.length);}
  async function loadJson(path){const r=await fetch(new URL(path,window.location.href),{cache:'no-store'});if(!r.ok)throw new Error(`${path}: HTTP ${r.status}`);return r.json();}
  async function loadKnowledge(){for(const path of ['../data/live-news.json','../data/demo-kb.json']){try{const d=await loadJson(path);const docs=Array.isArray(d)?d:d?.documents;if(Array.isArray(docs)&&docs.length)return docs;}catch(e){console.warn('NOVEN knowledge source unavailable:',path,e);}}return[];}
  function score(q,doc){const ts=new Set(normalize(q).split(/\s+/).filter((x)=>x.length>1));if(!ts.size)return 0;const title=normalize(doc.title),body=normalize(doc.text||doc.description||'');let hits=0;for(const t of ts){if(title.includes(t))hits+=1.5;else if(body.includes(t))hits+=1;}return Math.min(1,hits/ts.size);}
  function findBuiltIn(q){const n=normalize(q);return builtIn.find((item)=>item.keys.some((k)=>n.includes(normalize(k))));}
  async function publicReference(q){const language=lang();const endpoint=`https://${language}.wikipedia.org/w/api.php?action=query&origin=*&format=json&list=search&srsearch=${encodeURIComponent(q)}&srlimit=3`;const r=await fetch(endpoint,{cache:'no-store'});if(!r.ok)throw new Error(`Wikipedia HTTP ${r.status}`);const d=await r.json();const first=d?.query?.search?.[0];if(!first?.title)return null;return{title:first.title,source:'Wikipedia',url:`https://${language}.wikipedia.org/wiki/${encodeURIComponent(first.title.replace(/ /g,'_'))}`,text:first.title,score:1};}
  function renderSources(items){sources.innerHTML=items.length?items.map((item,i)=>`<article class="source"><div><a href="${escapeHtml(item.url||'#')}" target="_blank" rel="noopener noreferrer">${escapeHtml(item.title||`Source ${i+1}`)}</a><small>${escapeHtml(item.source||'Reference')}</small></div><small>${item.score?Number(item.score).toFixed(2):'ref'}</small></article>`).join(''):'<div class="empty-evidence">No external source attached.</div>';const e=document.getElementById('evidenceState');if(e)e.textContent=items.length?`${items.length} sources`:'No sources';}
  async function hashText(text){if(!window.crypto?.subtle)return 'browser-demo';const d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(String(text)));return `0x${Array.from(new Uint8Array(d),(b)=>b.toString(16).padStart(2,'0')).join('')}`;}
  async function answerQuestion(q){const direct=findBuiltIn(q);if(direct){const localized=window.NOVEN_I18N?.response?.(direct.key);return{text:localized||direct.text,sources:[{...direct,score:1}]};}const docs=await loadKnowledge();const ranked=docs.map((doc)=>({...doc,score:score(q,doc)})).filter((doc)=>doc.score>=0.25).sort((a,b)=>b.score-a.score).slice(0,5);if(ranked.length){const lead=ranked[0];const tail=lang()==='ar'?'وجد NOVEN أدلة مطابقة في طبقة المعرفة الحالية.':'NOVEN found matching evidence in its current knowledge layer.';return{text:`${lead.text||lead.description||lead.title}\n\n${tail}`,sources:ranked};}try{const ref=await publicReference(q);if(ref){const tail=lang()==='ar'?'مرجع عام من ويكيبيديا.':'Public reference from Wikipedia.';return{text:`${ref.title}\n\n${tail}`,sources:[ref]};}}catch(e){console.warn('NOVEN public reference lookup unavailable:',e);}return{text:lang()==='ar'?'لم أتمكن من استرجاع مرجع عام موثوق لهذا السؤال حاليًا حاول طرح السؤال بصيغة أكثر تحديدًا':'I could not retrieve a reliable public reference for that question right now. Try a more specific question.',sources:[]};}
  async function optionalApi(q){if(!API_BASE)return null;try{const r=await fetch(`${API_BASE.replace(/\/$/,'')}/v1/chat`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q,top_k:6,language:lang(),direction:isRtl()?'rtl':'ltr'})});if(!r.ok)throw new Error(`AI API HTTP ${r.status}`);return await r.json();}catch(e){console.warn('NOVEN optional AI backend unavailable:',e);return null;}}
  async function submit(event){event.preventDefault();event.stopImmediatePropagation();const q=question.value.trim();if(!q)return;send.disabled=true;send.innerHTML='<span>Thinking</span><b>…</b>';result.classList.remove('hidden');answer.textContent='';sources.innerHTML='<div class="empty-evidence">NOVEN is searching…</div>';try{const apiResult=await optionalApi(q);const data=apiResult||await answerQuestion(q);const text=data.answer||data.text||'No response.';let out='';for(const part of String(text).split(/(\s+)/)){out+=part;answer.textContent=out;await sleep(part.trim()?5:1);}const evidence=data.sources||data.evidence||[];renderSources(evidence);const fullHash=await hashText(`${q}\n${text}\n${JSON.stringify(evidence)}`);const shortHash=`${fullHash.slice(0,26)}…`;document.getElementById('verificationHash')?.replaceChildren(document.createTextNode(shortHash));document.getElementById('web3Hash')?.replaceChildren(document.createTextNode(shortHash));window.NOVEN_TRUST_RUN={runId:`NVR-${Date.now().toString(36).toUpperCase()}`,question:q,answer:text,sources:evidence,outputHash:fullHash,createdAt:new Date().toISOString(),runtime:apiResult?'ai-backend':'browser-runtime',model:apiResult?.model||'NOVEN browser runtime',dataset:apiResult?.dataset||'NOVEN knowledge layer',language:lang()};window.dispatchEvent(new CustomEvent('noven:run',{detail:window.NOVEN_TRUST_RUN}));const chip=document.querySelector('.model-chip');if(chip)chip.textContent=apiResult?'NOVEN · AI backend':'NOVEN · Browser runtime';addHistory(q);}catch(e){console.error('NOVEN runtime error:',e);answer.textContent=lang()==='ar'?'حدث خطأ مؤقت في تشغيل NOVEN':'NOVEN encountered a temporary runtime error. Please try again.';renderSources([]);}finally{send.disabled=false;send.innerHTML='<span>Ask</span><b>↗</b>';}}
  form.addEventListener('submit',submit,true);document.querySelectorAll('.suggestion').forEach((b)=>b.addEventListener('click',()=>{question.value=b.dataset.question||'';updateCounter();form.requestSubmit();}));document.querySelectorAll('.mode').forEach((b)=>b.addEventListener('click',()=>{document.querySelectorAll('.mode').forEach((x)=>x.classList.remove('active'));b.classList.add('active');question.dataset.prefix=b.dataset.prefix||'';question.focus();}));document.querySelector('.new-chat')?.addEventListener('click',()=>{question.value='';updateCounter();result.classList.add('hidden');answer.textContent='';sources.innerHTML='';window.NOVEN_TRUST_RUN=null;document.getElementById('verificationHash')?.replaceChildren(document.createTextNode('Awaiting a run'));document.getElementById('web3Hash')?.replaceChildren(document.createTextNode('Awaiting a run'));document.getElementById('verificationSignature')?.replaceChildren(document.createTextNode('—'));question.focus();});question.addEventListener('input',updateCounter);window.addEventListener('load',updateCounter);
})();
