const form=document.getElementById('composer');const input=document.getElementById('prompt');const messages=document.getElementById('messages');document.getElementById('year').textContent=new Date().getFullYear();form?.addEventListener('submit',e=>{e.preventDefault();const text=input.value.trim();if(!text)return;addMessage('أنت',text,'user');input.value='';setTimeout(()=>addMessage('NASHHAL AI','هذه نسخة واجهة تجريبية فقط. عند ربط API الفعلي سيستخدم النظام النموذج وطبقة الاسترجاع والتوثيق المعتمدة في مشروع Nashhal AI.','assistant'),450)});function addMessage(label,text,type){const el=document.createElement('div');el.className=`msg ${type}`;el.innerHTML=`<small>${escapeHtml(label)}</small><p>${escapeHtml(text)}</p>`;messages.appendChild(el);messages.scrollTop=messages.scrollHeight}function escapeHtml(value){const div=document.createElement('div');div.textContent=value;return div.innerHTML}
